# faerie-game
Hannah's Faerie Game Repo. Developed with Godot.

First milestone:
Main menu → start game → dynamically load test level → dynamically instantiate player → run/jump around → return to menu.

Main Menu
     ↓
Game
     ↓
load level.tscn dynamically
     ↓
instantiate selected player
     ↓
add player to scene tree
     ↓
play
     ↓
unload level
     ↓
Main Menu
